new -s hg -n home
new-window -n talaria -c "$MERCURY_BASE_PATH/talaria"
new-window -n talaria_services -c "$MERCURY_BASE_PATH/talaria_services"
new-window -n focus-group-monitor -c "$MERCURY_BASE_PATH/focus-group-monitor"
splitw -h -c "$MERCURY_BASE_PATH/focus-group-monitor"
new-window -n livelabs -c "$MERCURY_BASE_PATH/livelabs"
new-window -n production -c "$HOME"
selectw -t 0

# vim:fileencoding=utf-8:ft=conf
